#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;

int main() {
	
	
	
	for (int x = 100; x <= 999; x++) {
	cout << pow(x, 3);
	for (int y = 100; x <= 999; x++){
		cout << pow(y, 3);
		for (int z = 100; x <= 999; x++){
		cout << pow(z, 3);
	}
	}
}
  

	
	
	return 0;
	
}
